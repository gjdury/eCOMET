% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/250813_eCOMET_build_V3.R
\name{filter_cosmic_structure}
\alias{filter_cosmic_structure}
\title{Filter SIRIUS structure (CSI:FingerID) annotations by COSMIC confidence score}
\usage{
filter_cosmic_structure(
  mmo,
  input = "sirius_annot",
  cosmic_mode = c("exact", "approx"),
  threshold = 0.5,
  fields = "auto",
  na_cosmic = TRUE,
  suffix = NULL,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{mmo}{An ecomet mmo object containing \code{mmo[[input]]} (a data.frame).}

\item{input}{Character. Name of the element on \code{mmo} to filter. Defaults to \code{"sirius_annot"}.}

\item{cosmic_mode}{Which COSMIC column to use. One of \code{"exact"} or \code{"approx"}.}

\item{threshold}{Numeric. Keep structures with COSMIC >= threshold.}

\item{fields}{Character vector of columns to NA-out when COSMIC < threshold.
If \code{"auto"} (default), uses a reasonable default set if present in the table.}

\item{na_cosmic}{Logical. If \code{TRUE} (default), also set the COSMIC value to \code{NA}
when the structure is filtered out.}

\item{suffix}{Optional character string appended to the created element name.}

\item{overwrite}{Logical. If \code{FALSE} (default) and the target element already exists, error.}

\item{verbose}{Logical. If \code{TRUE}, prints a concise summary.}
}
\value{
The updated \code{mmo} object, with a new element \code{mmo[[paste0("sirius_annot_filtered_", suffix)]]}.
}
\description{
Applies a minimum COSMIC confidence cutoff to SIRIUS structure predictions inside an
ecomet \code{mmo} object. The function reads a chosen annotation table from
\code{mmo[[input]]} (default \code{"sirius_annot"}), flags structure annotations below
\code{threshold} by setting selected structure-identification fields to \code{NA},
and stores the result as a new element on \code{mmo} named
\code{"sirius_annot_filtered_<suffix>"}.
}
\details{
Rows are never dropped. "Removing" a structure means setting selected fields
(e.g., SMILES/InChI/name/InChIkey2D) to \code{NA}.
}
\examples{
\dontrun{
mmo <- filter_cosmic_structure(mmo, input = "sirius_annot",
                              cosmic_mode = "approx", threshold = 0.5,
                              suffix = "COSMIC_exact_0.5", verbose = TRUE)
}

}
