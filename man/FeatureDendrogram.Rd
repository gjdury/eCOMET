% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/250813_eCOMET_build_V2_MC.R
\name{FeatureDendrogram}
\alias{FeatureDendrogram}
\title{Build a feature dendrogram from a dissimilarity matrix}
\usage{
FeatureDendrogram(
  mmo,
  distance = "dreams",
  features = NULL,
  method = "average",
  save_output = FALSE,
  outprefix = "feature_tree",
  layout = c("base", "circular"),
  npl_col = "NPC#class",
  group_col = "group",
  add_group_bars = TRUE,
  abundance_fun = c("mean", "sum")
)
}
\arguments{
\item{mmo}{The mmo object containing one or more dissimilarity matrices (e.g., \code{dreams.dissim})}

\item{distance}{Which dissimilarity matrix to use; one of "dreams", "cos", or "m2ds" (default: "dreams")}

\item{features}{Optional character vector of feature IDs to include; if NULL, all features in the matrix are used}

\item{method}{Clustering linkage method passed to \code{stats::hclust} (default: "average")}

\item{save_output}{Logical; if TRUE, write a Newick tree (\verb{<outprefix>.nwk}) and a PDF dendrogram (\verb{<outprefix>.pdf})}

\item{outprefix}{File prefix for outputs when \code{save_output} is TRUE (default: "feature_tree")}

\item{layout}{Choose "base" (original rectangular tree) or "circular" (ggtree-based circular layout with annotation rings)}

\item{npl_col}{Column in \code{mmo$sirius_annot} used to color tips (e.g., "NPC#class"); ignored if missing}

\item{group_col}{Column in \code{mmo$metadata} that defines sample groups for tip bars}

\item{add_group_bars}{Logical; when TRUE, draws stacked bars at the tips summarizing feature abundance by \code{group_col}}

\item{abundance_fun}{Aggregation used for group bars: "mean" (default) or "sum" over samples within a group}
}
\value{
A list with \code{hclust} (the clustering), \code{dendrogram}, \code{phylo} (ape::phylo object), and when \code{layout = "circular"} a \code{ggplot} named \code{plot}
}
\description{
Given a feature-to-feature dissimilarity matrix (e.g., DREAMS, cosine, MS2DeepScore),
cluster the features and optionally write a Newick tree for visualization in tools like iTOL.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
tree <- FeatureDendrogram(mmo, distance = "dreams", features = NULL, method = "average",
                          save_output = TRUE, outprefix = "dreams_tree")
\dontshow{\}) # examplesIf}
}
